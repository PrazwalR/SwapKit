[package]
name = "swap-kit-types"
version = "0.1.0"
edition = "2021"
description = "Shared types for the swap-kit DeFi intent engine"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
