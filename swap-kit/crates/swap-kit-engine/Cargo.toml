[package]
name = "swap-kit-engine"
version = "0.1.2"
edition = "2021"
description = "High-performance MEV simulation and quote aggregation engine for swap-kit"
license = "MIT"

[[bin]]
name = "swap-kit-engine"
path = "src/main.rs"

[lib]
name = "swap_kit_engine"
path = "src/lib.rs"

[dependencies]
# Shared types
swap-kit-types = { version = "0.1.2", }

# Async runtime
tokio = { version = "1", features = ["full"] }

# HTTP server
axum = { version = "0.7", features = ["json"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Parallel computation (for address mining)
rayon = "1.10"

# Hashing (keccak256 for CREATE2)
tiny-keccak = { version = "2", features = ["keccak"] }

# Error handling
anyhow = "1"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
clap = { version = "4.6.1", features = ["derive"] }

[dev-dependencies]
tokio = { version = "1", features = ["test-util"] }
